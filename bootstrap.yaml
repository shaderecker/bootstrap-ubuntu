- name: Root mode
  hosts: localhost
  become: yes
  roles:
    - updates
    - nano
    - app_icons
    - zsh
    - applications
    - cli_applications
    - grub
    - dropbox
    - music_manager
    - minecraft
    - select_editor

- name: User mode
  hosts: localhost
  roles:
    - nano
    - zsh
    - sudo
    - select_editor
