- name: Root mode
  hosts: localhost
  become: yes
  roles:
    - nano
    - app_icons
    - zsh
    - applications
    - grub

- name: User mode
  hosts: localhost
  roles:
    - nano
    - zsh
    - sudo
