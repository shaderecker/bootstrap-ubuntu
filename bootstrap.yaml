- name: Root mode
  hosts: localhost
  become: yes
  roles:
    - nano
    - app_icons
    - zsh
    - applications
    - cli_applications
    - grub
    - dropbox
    - music_manager

- name: User mode
  hosts: localhost
  roles:
    - nano
    - zsh
    - sudo
